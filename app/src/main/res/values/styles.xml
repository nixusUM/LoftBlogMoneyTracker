<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/fabcolor</item>
        >ite
    </style>
    <style name="Container">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="Container.Background">
        <item name="android:background">@color/primary_material_light</item>
    </style>
    <style name="Container.Background.Dark">
        <item name="android:background">@color/backGroundDark</item>
    </style>
</resources>
